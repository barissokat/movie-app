/**
 * Displays a loading spinner, indicating that an asynchronous operation is in progress.
 */
const LoadingSpinner = () => {
  return (<div className='c-loader--wrapper'><div className='c-loader' /></div>)
}

export default LoadingSpinner